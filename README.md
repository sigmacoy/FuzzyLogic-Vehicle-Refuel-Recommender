# FuzzyLogic-Vehicle-Refuel-Recommender

## A Python-based fuzzy logic inference system built from scratch for CS345 Intelligent Systems 1. It evaluates vehicle fuel levels and distance to the next gas station to calculate optimal refuel urgency using Mamdani. <br><br> Members:
### Mc Cauley B. Bacalla <br> Jahzeel Lanz N. Mercado

## Features
### • Custom Inference Engine: Built from scratch in pure Python (no fuzzy libraries used for inference). <br><br> • 2-Input Architecture: Evaluates Current Fuel Level and Distance to Next Gas Station. <br><br> • Visualizations: Generates 2D membership function plots and a 3D control surface plot using Matplotlib.

## Requirements
```python
# If command/terminal
pip install numpy scikit-fuzzy matplotlib

# If inside a Jupyter Notebook cell
!pip install numpy scikit-fuzzy matplotlib

# Save
cd {traverse to your .py file}
python3 -m venv .venv
source .venv/bin/activate
pip install numpy scikit-fuzzy matplotlib
python3 main.py
pip install networkx
pip install scipy
python3 main.py
```